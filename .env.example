# Supabase Configuration
# Get these values from https://app.supabase.com/project/_/settings/api

# Your Supabase project URL
SUPABASE_URL=https://your-project-id.supabase.co

# Service role key (has full access, bypasses RLS)
# WARNING: Keep this secret! Never commit the actual .env file
SUPABASE_SERVICE_KEY=your-service-role-key-here

# Optional: Anon key (for frontend, respects RLS)
# Not needed for current setup since backend uses service key
# SUPABASE_ANON_KEY=your-anon-key-here

# Server Configuration
PORT=3000
